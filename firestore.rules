rules_version = '2';
// Progress App → Firebase 專案 site-progress-app-8d6c2（禁止發布到 ci-inspection）
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    match /users/{userId} {
      allow read: if signedIn();
      allow write: if signedIn(); // 正式版應限 systemAdmin
    }

    match /projectMembers/{memberId} {
      allow read: if signedIn();
      allow write: if signedIn();
    }

    match /projects/{projectId} {
      allow read: if signedIn();
      allow write: if signedIn();

      match /buildings/{buildingId} {
        allow read: if signedIn();
        allow write: if signedIn();
      }

      match /categories/{categoryId} {
        allow read: if signedIn();
        allow write: if signedIn();
      }

      match /checklistItems/{itemId} {
        allow read: if signedIn();
        allow write: if signedIn();
      }

      match /meta/{docId} {
        allow read: if signedIn();
        allow write: if signedIn();
      }

      match /defects/{defectId} {
        allow read: if signedIn();
        allow create, update: if signedIn();
        allow delete: if false;
      }

      // refresh token 等敏感資料：僅 Admin SDK（Cloud Functions）可讀寫
      match /private/{docId} {
        allow read, write: if false;
      }
    }
  }
}
